# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required scopes: repo (or public_repo for public repositories only)
GITHUB_TOKEN=your_github_token_here

# Todoist API Token
# Find at: https://todoist.com/prefs/integrations
TODOIST_TOKEN=your_todoist_token_here

# Optional: Todoist Project ID
# If specified, tasks will be created in this project
# Otherwise, tasks will be created in your Inbox
TODOIST_PROJECT_ID=

# Optional: Todoist Section ID
# If specified, tasks will be created in this section within the project
# Note: Requires TODOIST_PROJECT_ID to be set
TODOIST_SECTION_ID=

# Optional: Request throttle delay in seconds (for combined script)
# Adds a delay between API requests to avoid GitHub's secondary rate limits
# Default: 0.5 seconds (adjust higher if still experiencing rate limit issues)
THROTTLE_DELAY_SECONDS=0.5
